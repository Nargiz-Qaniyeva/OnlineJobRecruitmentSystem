@model Pagination<SendMessage>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminF/Views/Shared/_AdminLayout.cshtml";
    int count = (Model.CurrentPage - 1) * 5;
}

 <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Send Messages Table</h4>
                  <a class="btn btn-success" asp-controller="contact" asp-action="Create" >Create</a>
                  <div class="table-responsive">
                    <table class="table">
                      <thead>
                        <tr>
                          <th>№</th>
                          <th>Name</th>
                          <th>Email</th>
                          <th>Subject</th>
                          <th>Message</th>
                          <th>Setting</th>
                        </tr>
                      </thead>
                      <tbody>
                        @foreach (var item in Model.Items)
                       {
                                count++;
                         <tr>
                              <td>@count</td>
                              
                              <td>@item.Name</td>
                              <td>@item.Email</td>
                              <td>@item.Subject</td>
                              <td>@item.Message</td>
                              <td>
                                  <a class="btn btn-info" asp-controller="contact" asp-action="detail" asp-route-id="@item.Id"><i class="mdi mdi-account-card-details"></i></a>
                              </td>
                        </tr>
                       }
                        
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
 </div>

   <div class="row justify-content-center">
	<ul class="pagination pagination-lg">
		@for (var i = 1; i <= Model.PageCount; i++)
		{
			<li class="page-item @(Model.CurrentPage==i? "disabled" : "") ">
				<a class="page-link" 
				asp-area="adminf" 
				asp-controller="contact" 
				asp-action="index" 
				asp-route-pageSize="@i">@i</a>
			</li>
		}
	</ul>
</div>